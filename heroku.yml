build:
  docker:
    web:
      context: Intelli-scan/backend
      dockerfile: Dockerfile
release:
  image: web
  command: 
    - .venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port $PORT
run:
  web: .venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port $PORT